version: '3.8'
services:
  openvpn:
    image: ghcr.io/wfg/openvpn
    container_name: openvpn
    ports:
      - "1194:1194/udp"
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun
    volumes:
      - ./openvpn-data:/etc/openvpn
    restart: unless-stopped